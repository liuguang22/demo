<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.CourseMapper">
    <insert id="insertSc" >
        INSERT INTO grade (s_id, c_id,c_grade)
        VALUES (#{sId}, #{cId}, #{cGrade})
    </insert>
    <delete id="deleteSc">
        delete from grade where s_id =#{sId} AND c_id =#{cId}
    </delete>

    <select id="getAllCourse" resultType="com.example.demo.entity.Course">
        select * from course where not exists(select course.c_id,c_name,c_credit,r_id,t_id,t_name,t_time from grade,course where grade.c_id=course.c_id AND grade.s_Id=#{sId})
    </select>
    <select id="getMyCourse" resultType="com.example.demo.entity.Course">
        select course.c_id,c_name,c_credit,r_id,t_id,t_name,t_time from grade,course where grade.c_id=course.c_id AND grade.s_Id=#{sId}
    </select>
    <select id="getMycj" resultType="com.example.demo.entity.Course">
        select course.c_id,c_name,c_credit,t_id,t_name,c_grade from grade,course where grade.c_id=course.c_id AND grade.s_Id=#{sId}
    </select>

</mapper>
